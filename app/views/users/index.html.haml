- content_for :title do
  Listing users

%table
  %tr
    %th Name
  - for user in @users
    %tr
      %td = h user.name
      %td = link_to 'Edit', edit_user_path(user)

= link_to 'New user', new_user_path
