<h1>New invoice</h1>

<%= error_messages_for :invoice %>

<% form_for(@invoice, :url => [@client, @invoice]) do |f| %>

	<%= render :partial => 'form', :object => f %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Cancel', client_line_items_path(@client) %>
